stages:
  - deploy

deploy_dag:
  stage: deploy
  image: docker:latest
  services:
    - docker:dind
  script:
    - echo "Deploying DAG to Airflow..."
    - docker context create local || true
    - docker cp dags/emart_scraper.py dag_deployment:/opt/airflow/dags/
  only:
    - main
